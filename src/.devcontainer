{
  "name": "EducateMeSL Codespace",
  "postCreateCommand": "mv src/package.json . || true && npm install",
  "forwardPorts": [5173],
  "postStartCommand": "npm run dev"
}
git add .devcontainer/devcontainer.json
git commit -m "Add Codespaces auto setup for EducateMeSL"
git push
